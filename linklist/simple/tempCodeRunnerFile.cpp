  
//     temp=temp->next;
// }
    // ttemp=temp->next;
    // temp->next=first;
    // ttemp->next=temp->next;
    // first->next=NULL;
    // ttemp=first;

    temp = first;          // current node
    // ttemp = null;          // previous node
    // p = null;              // next node

    while(temp != null)
    {